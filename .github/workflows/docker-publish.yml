name: Build, Push & Deploy

on:
  push:
    branches: [master]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,prefix=

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.HOSTINGER_SSH_HOST }}
          username: ${{ secrets.HOSTINGER_SSH_USER }}
          key: ${{ secrets.HOSTINGER_SSH_KEY }}
          port: 2222
          script: |
            set -e

            # Login to GHCR
            echo "${{ secrets.GHCR_PAT }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

            # Ensure Traefik dynamic router exists
            mkdir -p ~/traefik/dynamic
            cat > ~/traefik/dynamic/em-web-client.yml << 'EOF'
            http:
              routers:
                em-web-client:
                  rule: "Host(`client.calamity-online.cloud`)"
                  service: em-web-client
                  entryPoints:
                    - websecure
                  tls:
                    certResolver: letsencrypt
              services:
                em-web-client:
                  loadBalancer:
                    servers:
                      - url: "http://em-web-client:80"
            EOF

            # Deploy the container
            mkdir -p ~/em-web-client
            cd ~/em-web-client

            cat > docker-compose.yml << 'EOF'
            services:
              em-web-client:
                image: ghcr.io/connor93/em-web-client:latest
                container_name: em-web-client
                restart: unless-stopped
                networks:
                  - web
            networks:
              web:
                external: true
            EOF

            docker compose pull
            docker compose up -d --force-recreate
            echo "✅ Web Client deployed to client.calamity-online.cloud"
